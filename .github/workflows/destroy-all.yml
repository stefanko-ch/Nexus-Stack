name: Destroy All Infrastructure

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type "DESTROY" (uppercase) to confirm FULL destruction'
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: infrastructure
  cancel-in-progress: false

jobs:
  destroy:
    name: Destroy All Nexus-Stack Resources
    runs-on: ubuntu-latest
    if: github.event.inputs.confirm == 'DESTROY'
    env:
      TF_VAR_cloudflare_api_token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      TF_VAR_cloudflare_account_id: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      TF_VAR_cloudflare_zone_id: ${{ secrets.CLOUDFLARE_ZONE_ID }}
      TF_VAR_hcloud_token: ${{ secrets.HCLOUD_TOKEN }}
      TF_VAR_domain: ${{ secrets.DOMAIN }}
      TF_VAR_access_emails: ${{ secrets.ACCESS_EMAILS }}
      TF_VAR_infisical_token: ${{ secrets.INFISICAL_TOKEN }}
      TF_VAR_github_owner: ${{ github.repository_owner }}
      TF_VAR_github_repo: ${{ github.event.repository.name }}

    
    steps:
      - name: Validate confirmation
        run: |
          if [ "${{ github.event.inputs.confirm }}" != "DESTROY" ]; then
            echo "❌ Destruction cancelled. You must type 'DESTROY' (uppercase) to confirm."
            exit 1
          fi
          echo "⚠️  DESTRUCTION CONFIRMED - This will delete ALL infrastructure and state!"

      - name: Checkout
        uses: actions/checkout@v4

      - name: Install OpenTofu
        uses: opentofu/setup-opentofu@v1
        with:
          tofu_version: 1.10.0

      - name: Generate SSH key for OpenTofu
        run: |
          mkdir -p ~/.ssh
          ssh-keygen -t ed25519 -f ~/.ssh/id_ed25519 -N "" -C "github-actions@nexus-stack"
          chmod 600 ~/.ssh/id_ed25519
          chmod 644 ~/.ssh/id_ed25519.pub

      - name: Create R2 credentials from secrets
        run: |
          mkdir -p tofu tofu/control-plane tofu/stack
          cat > tofu/.r2-credentials << EOF
          R2_ACCESS_KEY_ID="${{ secrets.R2_ACCESS_KEY_ID }}"
          R2_SECRET_ACCESS_KEY="${{ secrets.R2_SECRET_ACCESS_KEY }}"
          EOF
          
          cat > tofu/backend.hcl << EOF
          endpoints = {
            s3 = "https://${{ secrets.CLOUDFLARE_ACCOUNT_ID }}.r2.cloudflarestorage.com"
          }
          EOF

      - name: Generate config.tfvars
        run: |
          # Stack config
          cat > tofu/stack/config.tfvars << EOF
          server_name     = "nexus"
          server_type     = "cax31"
          server_location = "fsn1"
          server_image    = "ubuntu-24.04"
          ssh_public_key_path  = "~/.ssh/id_ed25519.pub"
          ssh_private_key_path = "~/.ssh/id_ed25519"
          domain         = "${{ secrets.DOMAIN }}"
          admin_email    = "${{ secrets.ADMIN_EMAIL }}"
          admin_username = "admin"
          github_owner   = "${{ github.repository_owner }}"
          github_repo    = "${{ github.event.repository.name }}"
          EOF
          cat tofu/services.tfvars >> tofu/stack/config.tfvars
          
          # Control Plane config
          cat > tofu/control-plane/config.tfvars << EOF
          server_name     = "nexus"
          server_type     = "cax31"
          server_location = "fsn1"
          domain          = "${{ secrets.DOMAIN }}"
          admin_email     = "${{ secrets.ADMIN_EMAIL }}"
          github_owner    = "${{ github.repository_owner }}"
          github_repo     = "${{ github.event.repository.name }}"
          EOF

      - name: Initialize OpenTofu for both states
        run: |
          source tofu/.r2-credentials
          export AWS_ACCESS_KEY_ID="$R2_ACCESS_KEY_ID"
          export AWS_SECRET_ACCESS_KEY="$R2_SECRET_ACCESS_KEY"
          
          # Initialize Nexus Stack state
          cd tofu/stack
          tofu init -backend-config=../backend.hcl 2>/dev/null || echo "No existing Nexus Stack state"
          
          # Initialize Control Plane state
          cd ../control-plane
          tofu init -backend-config=../backend.hcl 2>/dev/null || echo "No existing Control Plane state"

      - name: Destroy Nexus Stack infrastructure
        run: |
          cd tofu/stack
          source ../.r2-credentials
          export AWS_ACCESS_KEY_ID="$R2_ACCESS_KEY_ID"
          export AWS_SECRET_ACCESS_KEY="$R2_SECRET_ACCESS_KEY"
          
          echo "🗑️  Destroying Nexus Stack (Server, Tunnel, Services)..."
          tofu destroy -var-file=config.tfvars -auto-approve 2>/dev/null || echo "  No Nexus Stack state to destroy"

      - name: Destroy Control Plane infrastructure
        run: |
          cd tofu/control-plane
          source ../.r2-credentials
          export AWS_ACCESS_KEY_ID="$R2_ACCESS_KEY_ID"
          export AWS_SECRET_ACCESS_KEY="$R2_SECRET_ACCESS_KEY"
          
          echo "🗑️  Destroying Control Plane (Pages, Worker, DNS)..."
          tofu destroy -var-file=config.tfvars -auto-approve 2>/dev/null || echo "  No Control Plane state to destroy"

      - name: Delete R2 secrets from GitHub
        env:
          GH_TOKEN: ${{ secrets.GH_SECRETS_TOKEN }}
        run: |
          if [ -n "$GH_TOKEN" ]; then
            echo "🔐 Cleaning up R2 secrets from GitHub..."
            gh secret delete R2_ACCESS_KEY_ID 2>/dev/null || true
            gh secret delete R2_SECRET_ACCESS_KEY 2>/dev/null || true
            echo "✅ R2 secrets deleted"
          else
            echo "⚠️  GH_SECRETS_TOKEN not configured - R2 secrets must be deleted manually"
            echo "   Run: gh secret delete R2_ACCESS_KEY_ID && gh secret delete R2_SECRET_ACCESS_KEY"
          fi

      - name: Cleanup R2 API tokens from Cloudflare
        run: |
          echo "🔐 Cleaning up R2 API tokens from Cloudflare..."
          # Find and delete all nexus-r2-terraform-state tokens
          TOKENS=$(curl -s "https://api.cloudflare.com/client/v4/user/tokens" \
            -H "Authorization: Bearer ${{ secrets.CLOUDFLARE_API_TOKEN }}" \
            | jq -r '.result[] | select(.name | startswith("nexus-r2-terraform-state")) | .id')
          
          if [ -n "$TOKENS" ]; then
            for token_id in $TOKENS; do
              echo "  Deleting token: $token_id"
              curl -s -X DELETE "https://api.cloudflare.com/client/v4/user/tokens/$token_id" \
                -H "Authorization: Bearer ${{ secrets.CLOUDFLARE_API_TOKEN }}" > /dev/null
            done
            echo "✅ R2 API tokens deleted"
          else
            echo "  No R2 tokens found to delete"
          fi

      - name: Confirm destruction
        run: |
          echo ""
          echo "🗑️  All infrastructure has been destroyed:"
          echo "   - Hetzner Cloud server"
          echo "   - Cloudflare Tunnel"
          echo "   - Cloudflare DNS records"
          echo "   - Cloudflare Access policies"
          echo "   - Control Plane (Pages + Worker)"
          echo "   - R2 state bucket and contents"
          echo "   - R2 API tokens from Cloudflare"
          echo "   - R2 GitHub Secrets (if GH_SECRETS_TOKEN configured)"
          echo ""
          echo "To redeploy, run the Deploy workflow."
