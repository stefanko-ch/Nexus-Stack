# =============================================================================
# Nexus-Stack .gitignore
# =============================================================================

# OpenTofu / Terraform
.terraform/
*.tfstate
*.tfstate.*
.terraform.lock.hcl
.terraform.tfstate.lock.info
crash.log
crash.*.log

# Sensitive config files
# - config.tfvars: gitignored (contains secrets for local use)
# - services.tfvars: committed (only service definitions, no secrets)
# - image-versions.tfvars: committed (Docker image versions)
*.tfvars
*.tfvars.json
!services.tfvars
!image-versions.tfvars
!config.tfvars.example

# Environment variables (secrets!)
.env
!.env.example

# R2 state backend (auto-generated by make init)
backend.hcl
.r2-credentials

# Override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI configuration
.terraformrc
terraform.rc

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor
*.swp
*.swo
*~
.idea/
.vscode/
.cursorrules

# Logs
*.log

# Stack secrets (auto-generated on server)
stacks/.env
stacks/*/.env
